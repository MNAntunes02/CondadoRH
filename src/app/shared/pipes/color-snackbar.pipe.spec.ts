import { ColorSnackbarPipe } from './color-snackbar.pipe';

describe('ColorSnackbarPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorSnackbarPipe();
    expect(pipe).toBeTruthy();
  });
});
